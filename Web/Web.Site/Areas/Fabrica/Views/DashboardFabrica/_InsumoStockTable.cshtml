@using HtmlHelpers.BeginCollectionItemCore
@using Web.Site.Areas

@model IEnumerable<Insumo>

@{ 
    int i = 0;
}
<div class="ibox">
    <div class="ibox-title">
        <h5>Top 5 Insumos con stock mas bajo</h5>
        <div class="ibox-tools">
            <a class="collapse-link">
                <i class="fa fa-chevron-up"></i>
            </a>
        </div>
    </div>
    <h2 class="d-none d-print-block">Top 5 Insumos con stock mas bajo</h2>
    <div class="ibox-content">
        <table class="table">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Id</th>
                    <th>Nombre</th>
                    <th>Descripcion</th>
                    <th>Stock</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {

                    @foreach (var item in Model)
                    {
                        i = i + 1;
                        <tr>
                            <td>@i</td>
                            <td>@item.Id</td>
                            <td>@item.Nombre</td>
                            <td>@item.Descripcion</td>
                            <td>@item.StockTotal@item.Unidad</td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="9" class="text-center text-monospace"><i class="fa fa-folder-open"></i> Sin datos</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
